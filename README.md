先下载依赖：

    pip install -r requirements.txt

在运行main脚本：

```
python main.py
```

应该除了获取指定股票（ts_code）的最新技术指标数据都能用了 因为数据库里面这个表还是空的 而且有个问题 就是获取的这个表会有非常多的空 因为数据可能还没出炉或者是数据量不够大 其他的你们先看看 应该是可以跑了 当然依赖可以会有点问题 先问问ai吧 我先死了


